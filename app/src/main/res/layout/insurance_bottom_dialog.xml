<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:id="@+id/insurance_bottom_dialog"
    android:backgroundTint="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/white"
        android:layout_gravity="center_horizontal">

        <ImageView
            android:id="@+id/close_insurance_dialog"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_keyboard_arrow_down"
            android:layout_gravity="center"
            android:layout_marginVertical="@dimen/value_10dp"/>

          <SearchView
            android:id="@+id/search_bar"
            android:layout_width="match_parent"
            android:layout_height="@dimen/value_60dp"
            android:layout_marginStart="@dimen/value_20dp"
            android:layout_marginEnd="@dimen/value_20dp"
            android:background="@drawable/search_bar_bg"
            android:queryHint="Search here..."
            android:iconifiedByDefault="false"/>

        <androidx.recyclerview.widget.RecyclerView
            android:layout_width="wrap_content"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:paddingTop="@dimen/value_10dp"
            android:layout_gravity="center_horizontal"
            android:id="@+id/insurance_recycler_view"
            android:clipToPadding="false"
            android:layout_marginTop="@dimen/value_10dp"/>
    </LinearLayout>
</LinearLayout>